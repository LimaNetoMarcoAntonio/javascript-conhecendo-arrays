// Definindo os arrays menuPrincipal e menuDeSobremesas
const menuPrincipal = ['Pizza', 'Hamburger', 'Salada'];
const menuDeSobremesas = ['Sorvete', 'Torta', 'Pudim'];

// Utilizando o método concat para criar um novo array menuCompleto contendo todos os elementos de menuPrincipal seguidos pelos elementos de menuDeSobremesas
const menuCompleto = menuPrincipal.concat(menuDeSobremesas);

// Exibindo o novo array menuCompleto
console.log(menuCompleto);


