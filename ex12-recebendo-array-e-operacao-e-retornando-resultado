// 12 - Crie uma função chamada executaOperacaoEmArray que recebe dois parâmetros: um array e uma função de callback que executa alguma operação matemática. 
// Essa função deve iterar por cada elemento do array e aplicar a função de callback em cada um dos elementos, imprimindo o resultado da operação no console.

// Limpa o console antes de executar o programa
console.clear(); // Isso é útil para que o console não fique poluído com saídas anteriores.

// Função callback usando arrow function
// Esta função multiplica um número por 10. 
// Uma função callback é uma função que é passada como argumento para outra função.
const multiplicaPorDez = (numero) => numero * 10; // Aqui, 'numero' é o parâmetro que receberá o valor a ser multiplicado.

// Função principal usando arrow function
// A função executaOperacaoEmArray recebe um array e uma função de operação como parâmetros.
const executaOperacaoEmArray = (array, operacao) => {
    // A função 'forEach' itera sobre cada elemento do array.
    array.forEach(elemento => {
        // Para cada 'elemento' no array, aplicamos a função de operação (que é passada como argumento).
        const resultado = operacao(elemento); // 'resultado' armazena o valor retornado pela função de operação.
        
        // Imprime o resultado da operação no console.
        // A string interpolada `${elemento} após a operação = ${resultado}` formata a saída para mostrar o valor original e o resultado.
        console.log(`${elemento} após a operação = ${resultado}`);
    });
};

// Chamando a função com um array de números e a função de multiplicação
// Aqui, estamos passando um array com os números 1, 2 e 3, e a função 'multiplicaPorDez' como argumentos.
executaOperacaoEmArray([1, 2, 3], multiplicaPorDez);