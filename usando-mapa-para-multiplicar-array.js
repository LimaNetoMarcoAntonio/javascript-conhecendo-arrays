// Limpa o console antes de executar o programa
console.clear();

// Definindo o array de números
const arrayNums = [1, 2, 3, 4];

// Usando o método map para multiplicar cada número por 10
const numerosMultiplicados = arrayNums.map(numero => numero * 10);

// Exibindo o array de números multiplicados
console.log(numerosMultiplicados);