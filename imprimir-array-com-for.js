// Limpa o console antes de executar o programa
console.clear();

// Definindo o array de números ímpares
const numerosImpares = [1, 3, 5, 7, 9];

// Loop para iterar sobre o array de números ímpares
for (let i = 0; i < numerosImpares.length; i++) {
    // Imprime o elemento atual do array
    console.log(numerosImpares[i]);
}