// Unificando arrays

// Definindo as salas de estudantes
const salaUm = ['João', 'Juliana', 'Ana', 'Caio'];
const salaDois = ['Luiz', 'Pedro', 'Carlos', 'Paulo'];

// Unificando as salas de estudantes com o método concat
const salaUnificada = salaUm.concat(salaDois);

// Exibindo a lista de estudantes unificada
console.log(salaUnificada);
